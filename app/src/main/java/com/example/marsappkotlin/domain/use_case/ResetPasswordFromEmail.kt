package com.example.marsappkotlin.domain.use_case

import com.example.marsappkotlin.domain.RemoteRepository

class ResetPasswordFromEmail(private val remoteRepository: RemoteRepository) {
//    suspend operator fun invoke(email: String): String {
//        return remoteRepository.resetPasswordFromEmail(email)
//    }
}